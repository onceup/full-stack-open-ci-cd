name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Current date
        run: |
          date
      - name: Current directory contents
        run: |
          ls -l
